Pantheon+ curated copies:

    - pantheon_plus_mb_corr.csv
      SHA256: (fill after copy)
      Columns (pinned): z_col=?, data_col=? (mode=mu or mb_corr)
    - PantheonPlus_cov_SPD.npy
      SHA256: (fill after copy)
      Semantics: 'precision' or 'covariance'

    Notes:
    - Always update these fields after placing files.
